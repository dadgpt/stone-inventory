{
  "buildCommand": "cd client && npm install && npm run build && mkdir -p dist/images && cp -r ../public/images/* dist/images/",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
